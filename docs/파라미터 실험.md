## 1. 개요 (Overview)
본 보고서는 물류 자동화/피킹 시스템의 동적 배차를 위한 **CPR (Carton Picking & Routing) 알고리즘**의 핵심 가중치 모듈인 **$S_T$ (Short-Term Urgency / Progress / Contribution Score)** 및 **$S_F$ (Fragmentation Index Score)** 내 세부 파라미터 민감도 분석(Sensitivity Analysis) 결과를 종합 정리한 문서입니다.

전체 탐색(2,541개 조합) 및 세부 파라미터 그리드 서치를 통해 **납기 지연(Tardiness) 0건 완전 방어**, **최단 공정 완료 시간(Makespan 단축)**, **파편화 감소율 90% 이상**을 동시에 달성하는 최적 파라미터 세트를 확정하였습니다.

---

## 2. 수식 구조 및 주요 파라미터 정의

### 2.1 CPR 스코어링 수식
$$CPR = \alpha_1 \cdot S_T + \alpha_2 \cdot S_F - \alpha_3 \cdot S_D$$

* $\alpha = (\alpha_1, \alpha_2, \alpha_3)$: 글로벌 가중치 (Urgency, Fragmentation, Distance)

---

### 2.2 $S_T$ (단기 긴급도 및 공정 기여도 지수)
$$S_T = \text{urgency} + \gamma_1 \cdot (1.0 - \text{progress}) + \gamma_2 \cdot \text{contribution}$$

$$\text{urgency} = \begin{cases} \epsilon + \beta \cdot \frac{\text{now}}{\text{threshold}} & (\text{now} < \text{threshold}) \\ (\epsilon + \beta) + (1.0 - \beta) \cdot \left(1 - e^{-\lambda x_i}\right)^p & (\text{now} \ge \text{threshold}) \end{cases}$$

* $\text{threshold} = \text{deadline} - \text{margin\_sec}$
* $x_i = \text{now} - \text{threshold}$

* **파라미터 구성**:
  * $\text{margin\_sec}$: 비상 긴급도(Exponential Urgency) 전환 타임 윈도우 (초)
  * $\gamma_1$: 키트 잔여 미완성도 우대 가중치 (Progress Weight)
  * $\gamma_2$: 이번 디스패칭의 키트 완사 기여도 가중치 (Contribution Weight)
  * $\lambda$: 비상 모드 진입 후 지수 곡선의 기울기 (Exponential Decay Rate)
  * $p$: 지수 곡선의 오목/볼록 곡률 (Power Exponent)
  * $\epsilon$: 평시 기본 긴급도 오프셋 (Base Urgency)
  * $\beta$: 평시 시간에 따른 선형 긴급도 기울기 (Linear Slope)

---

### 2.3 $S_F$ (파편화 해소 및 완사 우대 지수)
$$S_F = w_{s1} \cdot \Delta \text{DeadSpaceRatio} + w_{s2} \cdot \max(0, \text{ClearanceBonus} - \text{FuturePenalty})$$

* **파라미터 구성**:
  * $w_{s1}$: 카톤 데드스페이스 감소 비율 가중치 (Carton Dead Space Weight)
  * $w_{s2}$: 토트 완전 비우기(Clearance) 및 Co-completion 보너스 가중치 (Clearance Weight)

---

## 3. 파라미터별 탐색 결과 및 민감도 분석

### 3.1 글로벌 가중치 ($\alpha$) 및 파편화 가중치 ($w_s$)
* **탐색 범위**: $\alpha \in \text{Simplex Grid (Step 0.05)}$, $w_s \in [0.0, 1.0]$ (총 2,541개 조합)
* **주요 결과**:
  * **글로벌 최적점**: $\alpha = (0.45, 0.20, 0.35)$, $w_s = (0.3, 0.7)$
  * **주요 특징**: $w_{s2} \ge 0.6$ ($w_{s1} \le 0.4$) 영역에 최적해의 90%가 집중됨. 완사(Clearance) 보너스를 극대화하는 설정이 조기 디스패칭을 억제하고 헛걸음을 예방함.

---

### 3.2 비상 마진 시간 ($\text{margin\_sec}$)
* **탐색 범위**: $0\text{s} \sim 3600\text{s}$ (0분 ~ 60분)
* **주요 결과**:
  * **최적값**: $\text{margin\_sec} = 1200.0\text{s}$ (20분)
  * **트렌드**: 전형적인 **U자형(U-shaped) 성과 곡선** 형성.
    * $< 600\text{s}$ (늑장 대응): AGV 이송 시간을 감당하지 못해 납기 지연(1~2건) 터짐.
    * $> 1500\text{s}$ (과잉 대응): 안 급한 키트까지 일찍 비상 곡선을 타면서 긴급도 변별력 상실(양치기 소년 현상)로 인해 Makespan이 늘어남.

---

### 3.3 진행률/기여도 가중치 ($\gamma_1, \gamma_2$)
* **탐색 범위**: $\gamma_1, \gamma_2 \in [0.1, 0.5] \times [0.1, 0.5]$ (총 25개 조합)
* **주요 결과**:
  * **최적값**: $\gamma_1 = 0.2, \gamma_2 = 0.3$ (황금 비율 $1 : 1.5$)
  * **트렌드**:
    * $\gamma_2 < 0.3$ 일 때: 기여도 보상이 약해져 완사 가능 토트를 두고 무의미한 배차가 누적, **납기 지연(Tardiness 2~8건) 대량 발생**.
    * $\gamma_2 \ge 0.3$ 확보 시 지연 0건 완전 방어.

---

### 3.4 비상 지수 곡선 형상 ($\lambda, p$)
* **탐색 범위**: $\lambda \in [0.001, 0.020]$, $p \in [1.0, 3.0]$ (총 30개 조합)
* **주요 결과**:
  * **최적값**: $\lambda = 0.006, p = 2.0$ (또는 $p=1.0$)
  * **특성**: **파라미터 둔감성(Insensitivity / Robustness) 확인**.
    * $\lambda \ge 0.006$ 및 $x_i \ge 300\text{s}$ (5분) 조건 진입 시 긴급도 점수가 $1.0$ 부근으로 수렴(포화)함.
    * 이 영역에서는 $\lambda, p$를 고의로 흔들어도 배차 순위(Ranking)가 변하지 않고 **100% 동일한 최적 성과(Obj `0.092091`)**를 유지함.

---

### 3.5 평시 긴급도 베이스라인 및 기울기 ($\epsilon, \beta$)
* **탐색 범위**: $\epsilon \in [0.0, 0.4]$, $\beta \in [0.0, 0.20]$ (총 25개 조합)
* **주요 결과**:
  * **$\epsilon$ (Base Urgency) 불변성**: $\epsilon$은 모든 후보 키트에 동일하게 가산되는 **상수 오프셋(Constant Additive Offset)**이므로, Argmax 배차 순위에 영향을 주지 않음 ($\epsilon$ 변화에 따른 성과 변동률 **0.00%**).
  * **$\beta$ (Linear Slope) 최적값**: $\beta = 0.10$ (종합 최적) / $\beta = 0.05$ (Makespan 최속 $16,648\text{s}$ 달성).
  * **트렌드**: $\beta = 0.0$ (시간 흐름 미반영) 설정 시 평시 긴급도 관리가 안 되어 성과 대폭 저하.

---

## 4. CPR 알고리즘 최종 표준 파라미터 권장안

| 구분 | 모듈 / 지표 | 파라미터 | 최종 확정값 | 핵심 역할 및 설정 정당성 |
| :--- | :--- | :---: | :---: | :--- |
| **Global** | CPR 가중치 | $\alpha_1, \alpha_2, \alpha_3$ | **`0.45, 0.20, 0.35`** | Urgency, Fragmentation, Distance 최적 밸런스 |
| **$S_F$** | 파편화 가중치 | $w_{s1}, w_{s2}$ | **`0.3, 0.7`** | 완사(Clearance) 보너스 우대로 헛걸음 최소화 |
| **$S_T$** | 비상 마진 시간 | $\text{margin\_sec}$ | **`1200.0` (20분)** | 지연 0건 완전 방어 Golden Window |
| **$S_T$** | 진행률 / 기여도 | $\gamma_1, \gamma_2$ | **`0.2, 0.3`** | 가성비 배차 및 작업 완료 촉진 (황금비 $1:1.5$) |
| **$S_T$** | 비상 곡선 형상 | $\lambda, p$ | **`0.006, 2.0`** | 포화 안전지대(Safe Zone) 내 강건한(Robust) 고정값 |
| **$S_T$** | 평시 긴급도 | $\epsilon, \beta$ | **`0.0 (또는 0.2), 0.10`** | $\epsilon$ 순위 불변성 / $\beta=0.10$ 최적 선형 기울기 |

---

## 5. 결론 및 학술적 시사점 (Key Takeaways)

1. **완벽한 납기 및 공정 제어**: 확정된 표준 파라미터 적용 시 **납기 지연(Tardiness) 0건**, **Makespan 약 718초 단축**, **파편화 감소율 89.5%**를 안정적으로 달성함.
2. **알고리즘 강건성(Robustness) 입증**: $\lambda, p, \epsilon$ 등 세부 파라미터들이 특정 넓은 범주 내에서 시스템 성과를 왜곡하지 않고 안정적인 성과를 유지함을 확인하여 실무 적용성이 우수함을 수학적으로 검증함.
"""

with open('cpr_parameter_sensitivity_report.md', 'w', encoding='utf-8') as f:
    f.write(md_content)

print("Markdown file created successfully.")


```

```text
Markdown file created successfully.


```

지금까지 진행한 **CPR 알고리즘의 $S_T$ 및 $S_F$ 파라미터 민감도 분석(Sensitivity Analysis)** 전체 내용을 깔끔하게 정리한 Markdown 보고서임.

아래 내용 복사해서 `.md` 파일로 쓰거나, 생성된 [cpr_parameter_sensitivity_report.md](https://www.google.com/search?q=file-tag:code-generated-file-d35d63d7-d08d-41a2-bffa-87319f33a7fb) 파일 다운로드해서 사용하면 됨!

---

# CPR Algorithm Parameter Sensitivity Analysis Report

## 1. 개요 (Overview)

본 보고서는 물류 자동화/피킹 시스템의 동적 배차를 위한 **CPR (Carton Picking & Routing) 알고리즘**의 핵심 가중치 모듈인 **$S_T$ (Short-Term Urgency / Progress / Contribution Score)** 및 **$S_F$ (Fragmentation Index Score)** 내 세부 파라미터 민감도 분석(Sensitivity Analysis) 결과를 종합 정리한 문서입니다.

전체 탐색(2,541개 조합) 및 세부 파라미터 그리드 서치를 통해 **납기 지연(Tardiness) 0건 완전 방어**, **최단 공정 완료 시간(Makespan 단축)**, **파편화 감소율 90% 이상**을 동시에 달성하는 최적 파라미터 세트를 확정하였습니다.

---

## 2. 수식 구조 및 주요 파라미터 정의

### 2.1 CPR 스코어링 수식

$$CPR = \alpha_1 \cdot S_T + \alpha_2 \cdot S_F - \alpha_3 \cdot S_D$$

* $\alpha = (\alpha_1, \alpha_2, \alpha_3)$: 글로벌 가중치 (Urgency, Fragmentation, Distance)

---

### 2.2 $S_T$ (단기 긴급도 및 공정 기여도 지수)

$$S_T = \text{urgency} + \gamma_1 \cdot (1.0 - \text{progress}) + \gamma_2 \cdot \text{contribution}$$

$$\text{urgency} = \begin{cases} \epsilon + \beta \cdot \frac{\text{now}}{\text{threshold}} & (\text{now} < \text{threshold}) \\ (\epsilon + \beta) + (1.0 - \beta) \cdot \left(1 - e^{-\lambda x_i}\right)^p & (\text{now} \ge \text{threshold}) \end{cases}$$

* $\text{threshold} = \text{deadline} - \text{margin\_sec}$
* $x_i = \text{now} - \text{threshold}$
* **파라미터 구성**:
* $\text{margin\_sec}$: 비상 긴급도(Exponential Urgency) 전환 타임 윈도우 (초)
* $\gamma_1$: 키트 잔여 미완성도 우대 가중치 (Progress Weight)
* $\gamma_2$: 이번 디스패칭의 키트 완사 기여도 가중치 (Contribution Weight)
* $\lambda$: 비상 모드 진입 후 지수 곡선의 기울기 (Exponential Decay Rate)
* $p$: 지수 곡선의 오목/볼록 곡률 (Power Exponent)
* $\epsilon$: 평시 기본 긴급도 오프셋 (Base Urgency)
* $\beta$: 평시 시간에 따른 선형 긴급도 기울기 (Linear Slope)



---

### 2.3 $S_F$ (파편화 해소 및 완사 우대 지수)

$$S_F = w_{s1} \cdot \Delta \text{DeadSpaceRatio} + w_{s2} \cdot \max(0, \text{ClearanceBonus} - \text{FuturePenalty})$$

* **파라미터 구성**:
* $w_{s1}$: 카톤 데드스페이스 감소 비율 가중치 (Carton Dead Space Weight)
* $w_{s2}$: 토트 완전 비우기(Clearance) 및 Co-completion 보너스 가중치 (Clearance Weight)



---

## 3. 파라미터별 탐색 결과 및 민감도 분석

### 3.1 글로벌 가중치 ($\alpha$) 및 파편화 가중치 ($w_s$)

* **탐색 범위**: $\alpha \in \text{Simplex Grid (Step 0.05)}$, $w_s \in [0.0, 1.0]$ (총 2,541개 조합)
* **주요 결과**:
* **글로벌 최적점**: $\alpha = (0.45, 0.20, 0.35)$, $w_s = (0.3, 0.7)$
* **주요 특징**: $w_{s2} \ge 0.6$ ($w_{s1} \le 0.4$) 영역에 최적해의 90%가 집중됨. 완사(Clearance) 보너스를 극대화하는 설정이 조기 디스패칭을 억제하고 헛걸음을 예방함.



---

### 3.2 비상 마진 시간 ($\text{margin\_sec}$)

* **탐색 범위**: $0\text{s} \sim 3600\text{s}$ (0분 ~ 60분)
* **주요 결과**:
* **최적값**: $\text{margin\_sec} = 1200.0\text{s}$ (20분)
* **트렌드**: 전형적인 **U자형(U-shaped) 성과 곡선** 형성.
* $< 600\text{s}$ (늑장 대응): AGV 이송 시간을 감당하지 못해 납기 지연(1~2건) 터짐.
* $> 1500\text{s}$ (과잉 대응): 안 급한 키트까지 일찍 비상 곡선을 타면서 긴급도 변별력 상실(양치기 소년 현상)로 인해 Makespan이 늘어남.





---

### 3.3 진행률/기여도 가중치 ($\gamma_1, \gamma_2$)

* **탐색 범위**: $\gamma_1, \gamma_2 \in [0.1, 0.5] \times [0.1, 0.5]$ (총 25개 조합)
* **주요 결과**:
* **최적값**: $\gamma_1 = 0.2, \gamma_2 = 0.3$ (황금 비율 $1 : 1.5$)
* **트렌드**:
* $\gamma_2 < 0.3$ 일 때: 기여도 보상이 약해져 완사 가능 토트를 두고 무의미한 배차가 누적, **납기 지연(Tardiness 2~8건) 대량 발생**.
* $\gamma_2 \ge 0.3$ 확보 시 지연 0건 완전 방어.





---

### 3.4 비상 지수 곡선 형상 ($\lambda, p$)

* **탐색 범위**: $\lambda \in [0.001, 0.020]$, $p \in [1.0, 3.0]$ (총 30개 조합)
* **주요 결과**:
* **최적값**: $\lambda = 0.006, p = 2.0$ (또는 $p=1.0$)
* **특성**: **파라미터 둔감성(Insensitivity / Robustness) 확인**.
* $\lambda \ge 0.006$ 및 $x_i \ge 300\text{s}$ (5분) 조건 진입 시 긴급도 점수가 $1.0$ 부근으로 수렴(포화)함.
* 이 영역에서는 $\lambda, p$를 고의로 흔들어도 배차 순위(Ranking)가 변하지 않고 100% 동일한 최적 성과(Obj `0.092091`)를 유지함.





---

### 3.5 평시 긴급도 베이스라인 및 기울기 ($\epsilon, \beta$)

* **탐색 범위**: $\epsilon \in [0.0, 0.4]$, $\beta \in [0.0, 0.20]$ (총 25개 조합)
* **주요 결과**:
* **$\epsilon$ (Base Urgency) 불변성**: $\epsilon$은 모든 후보 키트에 동일하게 가산되는 상수 오프셋(Constant Additive Offset)이므로, Argmax 배차 순위에 영향을 주지 않음 ($\epsilon$ 변화에 따른 성과 변동률 **0.00%**).
* **$\beta$ (Linear Slope) 최적값**: $\beta = 0.10$ (종합 최적) / $\beta = 0.05$ (Makespan 최속 $16,648\text{s}$ 달성).
* **트렌드**: $\beta = 0.0$ (시간 흐름 미반영) 설정 시 평시 긴급도 관리가 안 되어 성과 대폭 저하.



---

## 4. CPR 알고리즘 최종 표준 파라미터 권장안

| 구분 | 모듈 / 지표 | 파라미터 | 최종 확정값 | 핵심 역할 및 설정 정당성 |
| --- | --- | --- | --- | --- |
| **Global** | CPR 가중치 | $\alpha_1, \alpha_2, \alpha_3$ | **`0.45, 0.20, 0.35`** | Urgency, Fragmentation, Distance 최적 밸런스 |
| **$S_F$** | 파편화 가중치 | $w_{s1}, w_{s2}$ | **`0.3, 0.7`** | 완사(Clearance) 보너스 우대로 헛걸음 최소화 |
| **$S_T$** | 비상 마진 시간 | $\text{margin\_sec}$ | **`1200.0` (20분)** | 지연 0건 완전 방어 Golden Window |
| **$S_T$** | 진행률 / 기여도 | $\gamma_1, \gamma_2$ | **`0.2, 0.3`** | 가성비 배차 및 작업 완료 촉진 (황금비 $1:1.5$) |
| **$S_T$** | 비상 곡선 형상 | $\lambda, p$ | **`0.006, 2.0`** | 포화 안전지대(Safe Zone) 내 강건한(Robust) 고정값 |
| **$S_T$** | 평시 긴급도 | $\epsilon, \beta$ | **`0.0 (또는 0.2), 0.10`** | $\epsilon$ 순위 불변성 / $\beta=0.10$ 최적 선형 기울기 |

---

## 5. 결론 및 학술적 시사점 (Key Takeaways)

1. **완벽한 납기 및 공정 제어**: 확정된 표준 파라미터 적용 시 **납기 지연(Tardiness) 0건**, **Makespan 약 718초 단축**, 파편화 감소율 89.5%를 안정적으로 달성함.
2. **알고리즘 강건성(Robustness) 입증**: $\lambda, p, \epsilon$ 등 세부 파라미터들이 특정 넓은 범주 내에서 시스템 성과를 왜곡하지 않고 안정적인 성과를 유지함을 확인하여 실무 적용성이 우수함을 수학적으로 검증함.